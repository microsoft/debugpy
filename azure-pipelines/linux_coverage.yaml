variables:
    architecture: "x64"
    python.version: "3.8"

jobs:
    - job: 'Coverage'
      pool:
          name: Azure Pipelines
          demands: java
          vmImage: "ubuntu-16.04"
      steps:
        - template: "azure-pipelines/templates/coverage.yml"